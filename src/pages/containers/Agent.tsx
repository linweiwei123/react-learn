import * as React from 'react';
import ContentPlaceHolder from '../components/ContentPlaceholder';

const Agent = () => {
  return (
    <div>
      <ContentPlaceHolder pageName={"AGENT"} />
    </div>
  )
};

export default Agent;